jQuery(document).ready(function () {
	// var paragraph = jQuery("blockquote p").html();
	// var newHTML = paragraph.replace(paragraph.charAt(0), "<footer>— <cite>"+paragraph.charAt(0)+"</cite></footer>");
	// jQuery("blockquote p").html(newHTML);
});